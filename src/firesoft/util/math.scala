package firesoft.util

object math {
	
/*	
	
	def pow(a : Numeric[T], n:BigInt) : Numeric[T] = {
	  var acc = a
	  for (i <- n.bitLength-2 to 0 by -1){
	    acc = acc * acc
	    if(n.testBit(i))
	      acc = acc * a
	  }
	  acc
	}*/
}